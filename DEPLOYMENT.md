# 部署指南

## 一. GitHub Pages 部署

### 第一步：创建GitHub仓库
1. 访问 https://github.com/new
2. 仓库名称设置为 `ielts3`（或其他名称）
3. 设置为Public（公开）
4. 点击 "Create repository"

### 第二步：推送代码
```bash
# 进入项目目录
cd d:/Github/ielts3

# 初始化git（如果还未初始化）
git init

# 添加远程仓库
git remote add origin https://github.com/yourusername/ielts3.git

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Multi-module learning platform"

# 推送到主分支
git branch -M main
git push -u origin main
```

### 第三步：启用GitHub Pages
1. 进入仓库页面：https://github.com/yourusername/ielts3
2. 点击 Settings（设置）
3. 在左侧菜单找到 "Pages"
4. 在 "Build and deployment" 部分：
   - 选择 "Deploy from a branch"
   - 选择分支：main
   - 选择文件夹：/ (root)
5. 点击 Save

### 第四步：等待部署完成
- 部署通常需要1-2分钟
- 完成后，页面会显示你的网站URL
- 格式为：https://yourusername.github.io/ielts3

---

## 二. Firebase 配置

### 第一步：创建Firebase项目
1. 访问 https://console.firebase.google.com
2. 点击 "添加项目"
3. 输入项目名称（如 "ielts-learning"）
4. 选择位置
5. 创建项目

### 第二步：设置Web应用
1. 项目创建完成后，点击 "</>" 按钮添加Web应用
2. 输入应用名称（如 "IELTS Learning App"）
3. 点击 "注册应用"
4. 复制Firebase配置代码

### 第三步：更新配置文件
1. 打开 `js/firebase-config.js`
2. 替换以下配置（从Firebase控制台复制）：
```javascript
const firebaseConfig = {
  apiKey: "你的API密钥",
  authDomain: "你的项目.firebaseapp.com",
  projectId: "你的项目ID",
  storageBucket: "你的项目.appspot.com",
  messagingSenderId: "你的消息发送者ID",
  appId: "你的应用ID",
  measurementId: "你的测量ID"
};
```

### 第四步：启用认证方式
1. 在Firebase控制台，选择 "Authentication"（认证）
2. 点击 "Sign-in method"（登录方式）
3. 启用 "Google"：
   - 点击 Google 行
   - 打开 "启用"
   - 选择支持电子邮件
   - 点击 "保存"

### 第五步：设置Firestore数据库
1. 选择 "Firestore Database"
2. 点击 "Create database"
3. 选择位置（与项目同位置即可）
4. 选择安全规则模式：选择 "在测试模式下启动"（后续可优化规则）
5. 创建数据库

### 第六步：配置安全规则（重要）
为了保护用户数据，更新Firestore安全规则：

1. 进入Firestore Database → Rules标签
2. 替换为以下规则：
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 只允许已登录用户访问
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
```
3. 点击发布

### 第七步：获取授权URL（可选）
如果使用Google登录时遇到重定向问题：
1. 进入 Firebase 项目设置
2. 选择 "Authorization domains"（授权域）
3. 添加 `yourusername.github.io`

---

## 三. 测试部署

### 本地测试
```bash
# 使用Python内置服务器
python -m http.server 8000

# 或使用Node.js
npx http-server

# 访问 http://localhost:8000
```

### 功能测试清单
- [ ] 页面正常加载
- [ ] Google登录功能正常
- [ ] 可以添加任务
- [ ] 可以完成/删除任务
- [ ] 任务数据实时同步
- [ ] IELTS模块可以正常切换
- [ ] 数据保存正常
- [ ] 页面在手机上显示正常

---

## 四. 更新部署

### 更新代码后重新部署
```bash
# 添加更改
git add .

# 提交
git commit -m "描述你的更改"

# 推送
git push origin main
```

GitHub Pages会自动检测到更改并重新部署（通常需要1-2分钟）

---

## 常见问题排查

### 问题1：登录后仍显示登录界面
**原因**：Firebase配置错误或未正确初始化
**解决**：
1. 检查 `firebase-config.js` 中的配置是否正确
2. 打开浏览器开发者工具（F12）
3. 查看Console标签中的错误信息
4. 确认Firebase项目设置中已启用Google认证

### 问题2：页面无法访问
**原因**：GitHub Pages未正确配置
**解决**：
1. 确认仓库设置中已启用GitHub Pages
2. 确认选择的是 main 分支和 / (root) 文件夹
3. 等待2-3分钟后重新加载
4. 检查URL是否正确：https://yourusername.github.io/ielts3

### 问题3：数据无法同步
**原因**：Firestore数据库规则限制或网络问题
**解决**：
1. 检查网络连接
2. 查看浏览器控制台错误
3. 确认Firestore安全规则设置正确
4. 尝试清除浏览器缓存后重新登录

### 问题4：跨域问题（CORS）
**原因**：某些资源加载失败
**解决**：
1. 刷新页面
2. 检查CDN链接是否可用
3. 使用浏览器开发者工具检查具体错误

---

## 优化建议

### 1. 性能优化
- 启用 GitHub Pages 的 CDN 缓存
- 压缩 CSS 和 JavaScript 文件
- 使用图片优化工具

### 2. 安全性
- 定期更新 Firebase 安全规则
- 监控异常登录活动
- 定期备份数据

### 3. 用户体验
- 添加PWA支持（离线功能）
- 实现暗黑模式
- 添加多语言支持

---

## 支持联系

如有部署问题，请：
1. 检查 Firebase 和 GitHub 文档
2. 查看浏览器控制台错误信息
3. 提交GitHub Issue描述问题

祝你使用愉快！🎉
